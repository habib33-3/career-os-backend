export enum EnvironmentEnum {
    DEVELOPMENT = "development",
    PRODUCTION = "production",
    TEST = "test",
}

export enum logLevelEnum {
    ERROR = "error",
    WARN = "warn",
    INFO = "info",
    DEBUG = "debug",
}
